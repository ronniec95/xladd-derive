# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  vmImage: 'windows-latest'

variables:
  net_lib_path: $(Build.SourcesDirectory)/Rust/net_lib
  rust_release_path: $(Build.SourcesDirectory)/Rust/net_lib/target/release
  imageName: test-rust-repo

steps:
  - script: |
      curl https://sh.rustup.rs -sSf | sh -s -- -y
      echo "##vso[task.setvariable variable=PATH;]$PATH:$HOME/.cargo/bin"
    displayName: Install Build Mesh Core (Rust)
  - script: cargo build --release
    workingDirectory: '$(net_lib_path)'
  - task: CopyFiles@2
    inputs:
      Contents: |
        $(rust_release_path)/smart_monitor_server
        **/Channel.toml
      TargetFolder: $(build.artifactstagingdirectory)
      flattenFolders: true
  - publish: $(build.artifactstagingdirectory)
    artifact: Rust
    displayName: artifact $(build.artifactstagingdirectory)ß